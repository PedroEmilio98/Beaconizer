<%- include('../partials/header') %>

    <div class="starter-template">
        <h1>Avisos gerais</h1>
        <table>
            <% beacon.forEach(beacon=>{ %>
                <tr>
                    <td>
                        <%= beacon.title %>
                    </td>
                </tr>
                <% }) %>
        </table>
    </div>

    <%- include('../partials/footer') %>